---
whatsnew:
  title: "A Today screen you arrange yourself, stress on the home screen, and backups that check themselves"
  date: "September 2026"
  items:
    - "**Stress on your home screen (#2044, #2045).** A widget showing today's stress curve, on both platforms. It fills without needing the app opened, and when it has nothing to draw it says why instead of sitting blank (#2074)."
    - "**A Today screen you arrange yourself (#2047, #2051, #2053, #2054).** Today's stress curve and the Trends charts can now be placed on Today as cards, on both platforms. Tapping a hosted card opens the tab it came from rather than stranding you (#2052), and a hero ring on the classic Today opens its own detail (#2060)."
    - "**Charts that break where the strap did (#2064, #2083).** A heart-rate line no longer draws straight through hours the strap never recorded, on every surface that draws one. A gap now looks like a gap."
    - "**WHOOP 5 readings corrected (#2046, #2042, #2056, thanks @Trillient).** R-R intervals are read in the units the strap actually sends, which feeds HRV. Skin temperature now reaches recovery scoring before the score is computed rather than after. Both platforms carry tests covering the gaps these came from."
    - "**Backups that check themselves (#2086).** Every export path now verifies the file it actually wrote, so a truncated backup is caught when it is made instead of when you need it. When SQLite does complain, the message is shown in full and can be copied (#2084)."
    - "**A coach that holds on to your conversation (#2058, #2061).** A rejected API key can be corrected without losing the thread you were in. The coach is told what the workout was rather than only that one happened, the consent screen says what it actually sends (#2063), and the morning brief reaches the screen it was generated for (#2088)."
    - "**Diagnostics that say why, not just how many (#2080, #2094).** A night that missed the re-score cache now names the setting that dropped it. A dropped link is always recorded (#2065), the Live Console reads the device you actually selected (#2076), and the Rest card says why it is waiting on a sync (#2077)."
    - "**Your Oura serial stays out of the logs (#2095, #2090, thanks @pipiche38).** Serial numbers are masked in the Oura redactors on both platforms, and product-info replies no longer land in the raw diagnostics sidecar."
    - "**Sharing a Test Centre report (#2096).** The Test Centre now hands you the bundle through the same share sheet the strap log uses, and stops there, instead of steering you into opening an issue."
    - "**Smaller corrections.** Live heart rate stays armed while the Breathe screen is open (#2036, thanks @kiesstein). Workout actions stay readable over the daytime scene (#2050, thanks @kavemang). A silently dead duplicate string key is gone and the drifted Info.plist is regenerated (#2029, thanks @UtkuDenizAltiok). A manual workout can be entered by its start and end rather than only a duration (#2070), the strap picker seeds from the family actually recorded (#2067), and Today's heart-rate high and low read the samples rather than the mean curve (#2038)."
  title_locales:
    de: "Ein Heute-Bildschirm, den du selbst zusammenstellst, Stress auf dem Homescreen und Backups, die sich selbst prüfen"
    es: "Una pantalla de Hoy que organizas tú, el estrés en la pantalla de inicio y copias de seguridad que se comprueban solas"
    fr: "Un écran Aujourd'hui que vous composez vous-même, le stress sur l'écran d'accueil, et des sauvegardes qui se vérifient elles-mêmes"
    pt-PT: "Um ecrã Hoje que organiza a seu gosto, o stress no ecrã principal e cópias de segurança que se verificam a si próprias"
    zh: "可自行编排的「今天」页面、主屏幕上的压力曲线，以及会自我校验的备份"
    pl: "Ekran Dzisiaj, który układasz sam, stres na ekranie głównym i kopie zapasowe, które same się sprawdzają"
    ru: "Экран «Сегодня», который вы составляете сами, стресс на главном экране и резервные копии, которые проверяют себя сами"
---
# NOOP v11.6.0

A release about putting the day's readings where you actually look, and about being honest when a
reading is missing rather than drawing over the hole.

## A Today screen you arrange yourself

Today's stress curve and the Trends charts are no longer locked to their own tabs. Both can be hosted
on Today as cards, on Apple and on Android, so the screen you open first carries the things you check
first. A hosted card opens the tab it came from when you tap it, so it is a shortcut rather than a
dead end, and on the classic Today a hero ring now opens its own detail.

There is also a stress widget on both platforms, showing today's hourly curve on the home screen. It
fills on its own schedule without the app being opened, and when there is nothing to show it explains
why instead of leaving you looking at an empty box.

## Charts that break where the strap did

A heart-rate line used to draw straight across hours the strap never recorded, which quietly asserted
a reading that was never taken. Those runs are now broken wherever the gap is real, on every surface
that draws a heart-rate trace rather than only the one where it was first noticed.

## WHOOP 5 readings corrected

Thanks to @Trillient, R-R intervals from a WHOOP 5 are now read in the units the strap actually sends,
which is the input HRV is computed from. Skin temperature now reaches recovery scoring before the
score is computed rather than arriving after it. Both platforms carry regression tests covering the
gaps these were found in.

## Backups that check themselves

An export now verifies the file it actually wrote, on every path that writes one, so a backup that
was truncated is caught at the moment it is made instead of the moment you need it. When the database
itself reports a problem, the full text of what SQLite said is shown in something that can hold it and
copied, rather than clipped to a line that leaves out the part that mattered.

## A coach that holds on to your conversation

A rejected API key can be corrected in place without losing the conversation you were in. The coach is
told what the workout was rather than only that one happened, the consent screen describes what it
actually sends, and the morning brief now reaches the screen it was generated for.

## Diagnostics that say why

A night that missed the re-score cache now names the setting that dropped it, instead of reporting
only how many nights missed. A dropped link is always recorded, the Live Console reads the device you
selected rather than whichever field happened to be populated, and the Rest card says why it is
waiting on a sync instead of changing in silence.

Oura logs keep your serial out of them: serials are masked in the redactors on both platforms, and
product-info replies no longer reach the raw diagnostics sidecar. Both from @pipiche38.

The Test Centre now hands you its bundle through the same share sheet the strap log uses and stops
there, rather than steering you into opening an issue.

## Under the hood

Cross-platform parity now has a scanner, a reviewed authority and a baseline, contributed by @bhelm.
It inventories the declarations and constants that are supposed to match between the Swift and Kotlin
implementations and records the current accepted debt, so that regeneration cannot quietly accept
more. It does not yet gate product changes.

## Thanks

Thanks to @Trillient (WHOOP 5 R-R units, skin temperature ordering, regression tests), @pipiche38
(Oura serial masking and diagnostics sidecar), @bhelm (parity governance), @kiesstein (realtime heart
rate on the Breathe screen), @kavemang (readable workout actions), and @UtkuDenizAltiok (duplicate
string key and Info.plist drift).
